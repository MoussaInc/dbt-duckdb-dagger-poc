name: 'dagger_test'
version: '1.0.0'
config-version: 2

profile: 'dagger_test'

model-paths: ["models"]

seed-paths: ["seeds"]

clean-targets: ["target"]

models:
  dagger_test:
    staging:
      +materialized: view
    marts:
      +materialized: table
flags:
  require_generic_test_arguments_property: true